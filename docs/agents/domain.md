# Domain Docs

Engineering skills use a single-context domain model for this repository.

- `CONTEXT.md` defines shared domain vocabulary and contains no implementation details.
- `docs/adr/` records system-wide decisions that are hard to reverse, surprising without context, and the result of a real trade-off.
- Surface conflicts with existing ADRs explicitly and update the affected decision when its domain rules change.
